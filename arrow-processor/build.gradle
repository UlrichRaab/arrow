apply plugin: 'java'

dependencies {
    compileOnly project(':arrow')
    compile "org.apache.velocity:velocity:$rootProject.ext.velocityVersion"
}

sourceCompatibility = "1.7"
targetCompatibility = "1.7"
